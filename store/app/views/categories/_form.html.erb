<%= form_for @category do |f| %>
  <% f.object.errors.full_messages.each do |msg| %>
      <p><%= msg %></p>
  <% end %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>
